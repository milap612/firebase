specVersion: "v1beta"
serviceId: "firebase_demo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase_demo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
